# Core dependencies (most stable, cache-friendly)
requests>=2.31.0
numpy>=1.24,<2
setuptools>=78.1.1
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Discord and Bot dependencies
discord.py>=2.3.0

# Vector index (local). v2: embeddings come from the spark-gateway, so
# sentence-transformers + torch are dropped.
faiss-cpu==1.7.4

# Google Custom Search for fact-checking (lightweight). Gemini
# (google-generativeai) is dropped - chat/embeddings run on the spark-gateway.
google-api-python-client>=2.0.0

# Web framework dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
itsdangerous>=2.1.0
httpx>=0.25.0

# Monitoring and observability
sentry-sdk>=1.40.0

# Optional dependencies for development
watchdog>=3.0.0
