# Core dependencies (most stable, cache-friendly)
requests>=2.31.0
numpy>=1.24,<2
setuptools>=78.1.1
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Discord and Bot dependencies
discord.py>=2.3.0

# Vector index (local fallback). v2: embeddings come from the spark-gateway, so
# sentence-transformers + torch are dropped. When GRUGTHINK_DATABASE_URL is set,
# memory lives in Postgres+pgvector instead (see docs/pgvector-memory.md) and
# faiss is unused; it is kept for the no-database (dev/test) fallback backend.
faiss-cpu==1.8.0

# Postgres + pgvector backend for long-term memory. Reuses the CloudNativePG
# cluster grug already runs; one row per fact holds text + a vector(768), so
# there is no node-pinned PVC and no SQLite/FAISS pair to keep in sync.
psycopg[binary]>=3.1,<4
psycopg-pool>=3.2,<4
pgvector>=0.3,<1

# Google Custom Search for fact-checking (lightweight). Gemini
# (google-generativeai) is dropped - chat/embeddings run on the spark-gateway.
google-api-python-client>=2.0.0

# Web framework dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
itsdangerous>=2.1.0
httpx>=0.25.0

# Monitoring and observability. ddtrace activates via the `ddtrace-run`
# wrapper (Dockerfile CMD) - same convention as grug's own services - and
# feeds DD APM + LLM Obs (bot/llm_clients.py annotates the chat-completion
# span). Absent DD_LLMOBS_ENABLED (local dev/tests) it's a no-op exporter.
ddtrace>=3.5,<4
sentry-sdk>=1.40.0

# Optional dependencies for development
watchdog>=3.0.0
